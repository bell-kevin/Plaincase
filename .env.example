# Optional but required before a public AGPL deployment: use the exact public repository,
# release, or commit containing the deployed Corresponding Source.
# Never add secrets to VITE_* variables: Vite exposes them to the browser.
VITE_SOURCE_URL=https://github.com/your-name/plaincase
